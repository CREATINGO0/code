#include <stdio.h>
#include <stdlib.h>

#define UP    1
#define DOWN -1
#define 

void movup();
void movdown();
int reverse(int);
void printInf();

int main(void) {

    int curr = 0;
    int dir = UP;
    int marked[10] = {0};

    if( dir == UP ) {
    } else {
    }

    return 0;
}

void movup() {}
void movdown() {}
int reverse(int direction) { return ( direction == UP ? DOWN : UP); }
void printInf() {}
